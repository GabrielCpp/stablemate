[pytest]
testpaths = tests

# Autoclean. See base-library/workflows/coder/pytest.ini — pytest's default retains a
# passing test's sandbox for three whole runs, which these suites cannot afford.
tmp_path_retention_policy = failed
