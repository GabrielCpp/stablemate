# Hello World

Greet `{{ name }}` in one short, friendly sentence. Mention that the name has
{{ letters }} letters.

Do not read or write any files. This is the whole task.

## Output (JSON only)

```json
{"greeting": "Hello, world — seven letters is a fine name."}
```
