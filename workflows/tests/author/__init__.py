"""Makes `tests/author/` a package, so `author/test_workflow.py` and
`research/test_workflow.py` are two distinct test modules rather than a name collision
pytest reports as an import-file mismatch."""
