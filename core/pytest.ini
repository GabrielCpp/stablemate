[pytest]
testpaths = tests
# Discard a passing test's tmp_path immediately; keep it only for a failure, where it is
# still worth reading. pytest's default retains three whole runs' worth. See the note in
# base-library/workflows/coder/pytest.ini for what that costs a workflow suite.
tmp_path_retention_policy = failed
# A test suite that ships with warnings is a test suite with broken windows: the next
# person to add a test sees a warning they cannot interpret and assumes it is theirs,
# then learns to ignore the whole stream. Every warning is an error so a real one
# surfaces immediately.
filterwarnings =
    error
