# The implementation diff a trial materializes for the `seat-hold` story.
#
# `pre/` is story 1's post-image; `post/` is this story's, which story 3's `pre/` copies.
# The ledger and the page are untouched here: holding a seat is a transition and a route,
# and a story that also rewrote the storage layer would make an `unchanged` scenario
# ambiguous about which change it caught.
story: seat-hold
added: []
changed:
  - app/booking.py
  - app/service.py
